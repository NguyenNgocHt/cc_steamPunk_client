export type playerInfoPackage = {
  ID: number;
  playerName: string;
  avatarID: number;
  playerID: number;
  money: number;
};

export type playerInfo = {
  playerID: number;
  playerName: string;
  avatarID: number;
  money: number;
};
