export type playerInfoPackage = {
  ID: number;
  playerName: string;
  avatarID: number;
  playerID: number;
  money: number;
};

export type playerInfo = {
  userName: string;
  money: number;
  currency: string;
};
